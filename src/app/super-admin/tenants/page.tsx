import TenantsView from "./tenants-view";

export const metadata = {
  title: "Church Directory | Church OS Admin",
  description: "Manage all registered churches, subscription plans, and AI insights."
};

export default function TenantsPage() {
  return <TenantsView />;
}
